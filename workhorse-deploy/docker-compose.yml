services:
  course-api:
    build: ./api
    container_name: workhorse-course-api
    restart: unless-stopped
    networks:
      - core_default
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=postgresql://workhorse_user:${POSTGRES_PASSWORD:-changeme_secure}@postgres:5432/workhorse

networks:
  core_default:
    external: true
